 <!--
=========================================
; Title:  index.ejs
; Author: Faye Van Roekel
; Date:   14 September 2019
; Description: Index for EMS
;========================================
-->  
   
<!DOCTYPE html>
<html lang="en">
  <% include header %>
    
  <body>
  <% include navbar %>
    
  <div class="container">
      <h1>Employee Management System</h1>
          <p>
          This is a database system for human resources.  It is a confidential system which contains records of employees.
          </p>
  </div>

  <% if (employees.length) { %>
    <h2>Employees</h2>
    <br />
    <a class="" href="/new">New Employee</a>
    <br />
    <table class="table table-hover">
      <thead class="bg-secondary">
        <tr>
          <th class="text-white">Name</th>
          <th class="text-white">Functions</th>
        </tr>
      </thead>
      <tbody>
        <% employees.forEach(function (entry) { %>
        <tr>
          <td><%= entry.name %></td>
          <td style="width: 10%; text-align: center;">
            <a style="color: #555" href="/view/<%= entry.name %>">
              <i class="fa fa-pencil fa-fw" aria-hidden="true"></i>
            </a>
          </td>
        </tr>
      </tbody>
      <% }) %>
    </table>
    <% } else { %> No entries! <a href="/new">Add one!</a>
    <% } %>

    <br />
  </div>
  <% include scripts %>
</body>

</html>
    




   